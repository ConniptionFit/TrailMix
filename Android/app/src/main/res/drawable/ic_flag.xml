<?xml version="1.0" encoding="utf-8"?>
<!--
  CAP-24: "flag this moment" glyph for the capture top bar. Material's `Icons.Filled.Flag`
  is not in material-icons-core (REL-02 swapped extended for core to cut ~30 MB off the
  APK), so this is a local copy of the standard Material "flag" glyph, no tint baked in
  so the Compose `Icon` tint wins — same convention as ic_pause.xml.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M14.4,6L14,4H5v17h2v-7h5.6l0.4,2H19V6H14.4z" />
</vector>
