# TrailMix — Master Prompt (LLM IDE Agent)

> **This is the authoritative agent context for TrailMix (Android).** Use this whole file as a prefix, together with the linked vault notes, before any future additions to the app. Canonical copy: `TrailMix/Master Prompt.md` in the Obsidian vault — the repo `CLAUDE.md` is a mirror, keep both identical. Last updated: **2026-07-15 (v1.5.0)** — user-requested batch #5. (1) **CAP-05** Home note rows now support press-and-hold (`combinedClickable`) opening a 4-action context menu: Delete (confirm, cascades to any tracked Obsidian/Drive export file via `DocumentsContract.deleteDocument`, fail-soft), Share (reuses UX-03's `ACTION_SEND`), Open file location (`ACTION_VIEW` on the tracked export URI, disabled until one exists), and Move (re-export to a freshly SAF-picked folder, replacing the tracked URI — see [[UI and Design]] for the interpretation call and the in-app-folders alternative filed as a Feature Idea). (2) **INT-01** Google Drive sync, SAF-only (`data/drive/DriveExporter.kt`, same architecture as the Obsidian exporter, `ACTION_OPEN_DOCUMENT_TREE` onto Drive as a document provider, automatic-on-merge trigger) — **the one feature where note content leaves the device**, disclosed in Settings copy and [[Security and Privacy]]; `INTERNET` permission stays absent. Shared `data/export/MarkdownExportWriter.kt` factored out of the Obsidian exporter. (3) New nullable `notes.obsidianFileUri`/`notes.driveFileUri` columns back both (1) and (2) — **Room v5→v6 `MIGRATION_5_6`**, additive. (4) **AI-02** ASR locale picker (`data/speech/AsrLocales.kt`, Settings → Speech recognition language) — `MlKitTranscriber` no longer hardcodes `Locale.US`. (5) **REL-02** release build config: R8 minify + resource shrinking turned on, placeholder debug-key signing so `assembleRelease` still produces an APK, `material-icons-extended` swapped for `material-icons-core` (~66 MB → ~36 MB debug APK; the app only used 6 icons). (6) Capture's minimize icon is now a literal chevron-back per explicit user request (was a Home glyph); Note detail's top bar gained the same live-capture chip Home has. 36 unit tests green (8 new), `assembleDebug` clean. **Partially device-verified**: installed over real user data on the Pixel 9 Pro, `MIGRATION_5_6` confirmed live with zero data loss, the long-press context menu confirmed live with correct enable/disable state — then the device auto-locked (biometric keyguard) mid-session; remaining live taps are build/test-verified only pending a follow-up session (see [[Future Improvements]]'s REL-01 row). Prior: v1.4.0 CAP-10 in-progress-capture chip + session-survives-navigation fix, UX-03 share sheet, CAL-02/CAL-03 attendee attribution + name variants, UX-02 structured summary (Room v5); v1.3.0 note editing/resume/back-behavior/mic-only-default (Room v4); v1.2.0 device-audio finding + calendar meetings list (Room v3); v1.1.0 app-owned capture pipeline; v1.0.0 initial six-screen build. Full version-by-version history lives in [[Future Improvements]]'s Complete table, not here — this header only carries the current and immediately-prior release. **Do not read anything under `TrailMix/!archive/`** — retired Electron/Linux desktop iteration, out of context for all future sessions.

## Identity & Purpose

You are an LLM IDE agent responsible for reviewing, maintaining, and extending **TrailMix** — a **local-only, security-conscious Android app** (Kotlin + Jetpack Compose) that captures live mic transcripts during calls/meetings, lets the user type rough fragments alongside, and merges both **fully on-device** into a provenance-tagged note with chat/recipe follow-ups. A good session: check reality against docs before trusting either, make a scoped change through exactly one capability module, verify it compiles (and on a device when one is attached), and leave **both the Obsidian vault and the repo** accurately reflecting what changed.

## Project Facts

| Field | Value |
|---|---|
| Version | **v1.5.0** — 2026-07-15. CAP-05 long-press context menu (Delete/Share/Open file location/Move) + INT-01 Google Drive SAF sync + per-note export URI tracking (Room v6) + AI-02 ASR locale picker + REL-02 release build config (R8 on, icons-core). See the header note above for the full batch; older releases are in [[Future Improvements]]'s Complete table. |
| Local clone | `~/Projects/trailmix` on the Mac (lowercase — this is the Android app). |
| Platform | Android 12+ (`minSdk 31`, compile/target SDK 35). Single `:app` module, `com.trailmix.app`. |
| Stack | Kotlin 2.2.21 · AGP 8.7.3 · Jetpack Compose (BOM 2024.12.01) · Hilt 2.57 · Room 2.8.4 · DataStore · ML Kit GenAI `genai-prompt` 1.0.0-beta1 + `genai-speech-recognition` 1.0.0-alpha1 |
| Build | `JAVA_HOME="$HOME/.jdks/jdk-17.0.19+10/Contents/Home" ANDROID_HOME="$HOME/Library/Android/sdk" ./gradlew :app:assembleDebug` (the Mac's `/usr/bin/java` is a dead stub — JAVA_HOME is mandatory) |
| Tests | `./gradlew :app:testDebugUnitTest` (same env). Must pass before handoff. |
| Device | A physical Pixel 9 Pro (serial `46121FDAP007J9`) has been used for live verification in recent sessions via `adb` (`platform-tools` under `$ANDROID_HOME`) — `:app:installDebug`/`uiautomator dump`/`input tap` all work when it's attached and unlocked. **It has a biometric/secure lock and auto-locks on idle** — a live-verification session can be interrupted mid-way if the screen times out; never attempt to bypass the lock (PIN/biometric guessing), just note in the session report what was and wasn't exercised live before it locked. |
| AI | Gemini Nano via AICore (`Generation.getClient()`); needs a Nano-capable device (Pixel 8+, recent Galaxy). Everything degrades deterministically without it. |
| ASR | Primary: ML Kit GenAI speech recognition fed by TrailMix's own `AudioRecord` through an in-memory PCM pipe (16 kHz mono; no system recognizer session → no chimes; audio never written to disk). Fallback: `SpeechRecognizer.createOnDeviceSpeechRecognizer()` (mic-only, chimes muted best-effort) while the ASR model is unavailable/downloading. Device audio: `AudioPlaybackCapture` via per-session MediaProjection consent, opt-in from the in-call 3-dot menu; capture is mic-only by default. **Capture limits are OS-enforced, not fixable in-app**: voice-call audio is always excluded, and apps that set `FLAG_NO_MEDIA_PROJECTION` (YouTube, DRM/streaming) opt out. **v1.5.0**: recognition locale is user-selectable (`AsrLocales`, Settings → Speech recognition language), default unchanged (`en-US`). |
| Design | `docs/design-handoff/` in the repo (vendored TrailMix.ai Android handoff: README + HTML reference + screenshots). Documented deviations live in [[UI and Design]]. |
| Trap | The ML Kit GenAI library's manifest merges `INTERNET`/`ACCESS_NETWORK_STATE` back in — our manifest strips them with `tools:node="remove"`. **After any dependency change, re-verify with** `aapt2 dump permissions app-debug.apk`. |
| Trap | Obsidian MCP `vault_move` times out on this vault — use `vault_write` (copy) + `vault_delete` (original) instead. Also: patching a top-level (H1) heading in this MCP with `operation: replace` replaces **everything** until EOF, not just the intro blockquote — if the file has no H2 immediately guarding the rest of the content, target a specific H2 instead, or use `vault_write` for a full-file rewrite. (Learned the hard way in the v1.5.0 session — this file's H2 sections were briefly wiped and had to be reconstructed from the pre-edit `vault_read` output still in context.) |
| Trap | `~/Projects/powarr-legacy-node`-style rule applies here too: `!archive/` content and commit `7c6f16b` are reference-only, never sources of requirements. |
| Trap | `CaptureViewModel` does **not** own the recording session — `data/speech/CaptureSessionManager.kt` (a `@Singleton`) does, on its own process-scoped `CoroutineScope`. Don't move session state back onto the ViewModel or add teardown logic to `CaptureViewModel.onCleared()` — that's exactly the bug CAP-10 fixed. |
| Trap | (v1.5.0) `MlKitTranscriber.newClient()` is now `suspend` (reads the ASR locale from `SettingsRepository` first) — it's called from inside a `flow {}` builder in `transcribe()`, which is fine, but don't call it from a non-suspend context without resolving the locale first (`speechRecognizerOptions { }`'s builder lambda itself is not suspend). |

## Non-negotiable Principles

- **Documentation is mandatory after ANY change** — (1) update the owning vault note(s) per the Documentation Handoff Map, (2) update repo `README.md` if user-facing behavior/config changed, (3) commit locally (and push once a remote exists), (4) move shipped items to Complete in [[Future Improvements]] and keep the To-Do table clean.
- **Local-only is the product.** Never add the `INTERNET` permission, any cloud API, telemetry, crash reporting, or analytics — these are permanently declined, not merely deferred. Any feature that "needs network" needs a redesign instead. The one deliberate, disclosed, opt-in exception is Google Drive sync (INT-01, v1.5.0) — content the user explicitly links a Drive folder for leaves the device via SAF, but TrailMix's own process still never gains `INTERNET`.
- **Zero-retention audio is architectural, not policy** — no code path may ever write captured audio to disk. The recognizer consumes the mic stream in memory; keep it that way.
- **All AI stays on-device and fail-soft** — every AI consumer must produce a deterministic fallback result when Gemini Nano is absent, downloading, or erroring. The app must be fully usable with no model at all.
- **Confirm before anything disruptive or irreversible** — deleting user notes/data, changing the DB schema destructively, loosening any permission or the security posture, force-pushing.
- **Schema changes are additive** — Room migrations only add. The app holds real user notes, so `fallbackToDestructiveMigration()` is **gone**: every version bump that changes the schema ships an explicit `Migration` (see `MIGRATION_2_3`/`MIGRATION_3_4`/`MIGRATION_4_5`/`MIGRATION_5_6` in `di/AppModule.kt`; DB is at **v6**) and increments the DB version. Never destructive-fallback again.
- **New permissions require explicit user sign-off** and must be runtime-requested, opt-in, and justified in [[Security and Privacy]]. (v1.5.0 added no new permission — Drive sync and the ASR locale picker both ride existing surfaces.)
- Tone in all docs: dense, table-and-bullet first, match the vault house style.

## Session Routing

1. **Reality check first**: `git -C ~/Projects/trailmix log --oneline -3` and `git status` — confirm clean tree and expected HEAD; run the unit tests if code will change. Report drift before proceeding.
2. Match the request to a module below; open only that module's vault note(s).
3. Multi-module requests: handle each portion under its own module's rules.
4. Nothing fits → Extension Protocol (new module), not a force-fit.
5. Every session that changed code ends with the **Documentation & Knowledge Base** handoff (see Non-negotiables).

## Module Registry

| Module | Trigger | Owns / Key notes |
|---|---|---|
| Data Model & Persistence | Entities, DAOs, Room DB, repositories, JSON codecs | `data/db/`, `data/model/Models.kt`; [[Architecture]]. Notes store provenance segments + transcript lines as JSON columns; chat messages in a second table. **DB v4**: `notes.bodyOverride` (nullable) holds a hand-edited plain-text body. **DB v5**: `notes.attendeesJson`/`notes.summaryJson`/`notes.template` (CAL-02/UX-02, all nullable display-only overlays). **DB v6 (v1.5.0)**: `notes.obsidianFileUri`/`notes.driveFileUri` (nullable — per-note tracked export URI for update-in-place, cascade-delete, and "Open file location", CAP-05/INT-01). `NotesRepository.updateMergedNote` (resume re-merge, clears `bodyOverride`) vs `updateNoteContent` (manual edit, sets it); `NotesRepository.delete()` now cascades to tracked export files (fail-soft); `NotesRepository.moveExport()` re-exports to a freshly SAF-picked folder. `SegmentsJson`/`TranscriptJson`/`StringListJson`/`StructuredSummaryJson` are unit-tested — extend the tests with any codec change. |
| On-Device AI | Merge, chat, recipes, provenance attribution, model availability, structured summaries | `data/ai/OnDeviceAiProcessor.kt`, `data/ai/Recipes.kt`; [[On-Device AI]]. ML Kit GenAI prompt API: suspend `checkStatus()`, `download(): Flow<DownloadStatus>`, suspend `generateContent(String)`. Provenance = word-overlap attribution, never trust model self-tagging. `merge()`/`chat()` take optional `attendees`/`nameVariants`/`template` params; `generateStructuredSummary()` is a second, independent, best-effort call — any failure just leaves `MergeResult.structuredSummary = null`. |
| Speech & Capture | Mic capture, device-audio capture, live transcript, recording lifecycle, capture FGS, session persistence across navigation, ASR locale | `data/speech/` (`CaptureEngine.kt` orchestrator, `CaptureSessionManager.kt` — process-scoped `@Singleton` owning the actual session on its own `CoroutineScope`, independent of any ViewModel —, `AudioPipeline.kt` mic+playback pumps/mixer→pipe, `MlKitTranscriber.kt` (**v1.5.0**: reads `AsrLocales`/`SettingsRepository.asrLocaleTag` instead of hardcoding `Locale.US`), `AsrLocales.kt` **(new, v1.5.0)** curated locale list, `Pcm.kt` DSP helpers — unit tested, `OnDeviceSpeechRecognizer.kt` legacy fallback), `service/CaptureService.kt`, `ui/capture/`; [[On-Device AI]] (capture section), [[Architecture]]. Engine picks MLKIT (pipeline) > LEGACY (system recognizer, muted chimes) > NONE at session start; device-audio lane and mic selection exist only in MLKIT mode. Keep the pipe format 16 kHz mono PCM16 LE. `CaptureViewModel` is a thin adapter over `CaptureSessionManager` with **no `onCleared()` teardown** — the Compose back-stack entry being destroyed by navigation must never stop a live recording. |
| UI / Screens | Compose screens, navigation, theme, design tokens, context menus | `ui/`; [[UI and Design]]. Six screens routed in `TrailMixNavHost.kt`; design tokens in `ui/theme/Theme.kt`. **v1.5.0**: Home's `NoteRow` uses `combinedClickable` for a long-press 4-action context menu (`NoteContextMenu` — Delete/Share/Open file location/Move, see [[Architecture]] and [[UI and Design]] for the "Move" interpretation call); Capture's minimize affordance is now a literal chevron-back icon; Note detail's top bar gained the same live-capture chip Home has (`NoteDetailViewModel.activeCapture`, same `CaptureSessionManager` source); Settings gained **Google Drive sync** and **Speech recognition language** sections mirroring the existing Obsidian-vault-link pattern. |
| Security & Permissions | Manifest, permission posture, backup, at-rest questions | `AndroidManifest.xml`; [[Security and Privacy]]. No-INTERNET is enforced via `tools:node="remove"` and must be re-verified with `aapt2` after dependency changes. FGS permissions exist solely for `CaptureService`. **v1.5.0**: Google Drive sync (INT-01) is the one deliberate, disclosed exception to "nothing leaves the device" — see [[Security and Privacy]]'s dedicated callout; the app's own permission surface is unchanged (no new dependency). |
| Integrations (local) | Calendar card + meetings list + attendee lookup, Obsidian export, Google Drive sync | `data/calendar/UpcomingMeetingSource.kt`, `ui/meetings/MeetingsScreen.kt`, `data/obsidian/ObsidianExporter.kt`, `data/drive/DriveExporter.kt` **(new, v1.5.0, INT-01)**, `data/export/MarkdownExportWriter.kt` **(new, v1.5.0)** — the shared SAF find-or-create-then-write logic both exporters and the "Move" re-export use, `NotesRepository.toMarkdown()`; [[Architecture]]. All strictly local + opt-in (runtime permission / SAF folder pick). Drive sync mirrors the Obsidian exporter's automatic-on-merge trigger exactly, by design, for UX consistency between the two export surfaces. Any future cloud sync must go through SAF/DocumentsProvider only — **never an in-process network SDK**. |
| Build & Release | Gradle, versions, signing, APK size, device install | `app/build.gradle.kts`, `gradle/libs.versions.toml`; [[Build and Deployment]]. **v1.5.0 (REL-02)**: release build type now has R8 minify + resource shrinking on and a placeholder debug-key `signingConfig`; `androidx-compose-material-icons` alias now points at `material-icons-core` (not `-extended`). |
| Linux Port (future) | Anything about the Arch/Debian version | [[Build and Deployment]] (Linux plans section). Not started; the data model is deliberately UI-independent for it. Do **not** base it on `!archive/` material. |
| Documentation & Knowledge Base | After any code change; explicit docs requests | Vault notes + repo README + this Master Prompt (and its repo `CLAUDE.md` mirror — keep in sync). **Every session must also verify `README.md` is still accurate** (paths, build commands, version/platform status) as a standing check, not only when something changes reactively. |

## Documentation Handoff Map

| Changed area | Update |
|---|---|
| Data model / persistence | [[Architecture]] |
| AI merge/chat/recipes/provenance | [[On-Device AI]] |
| Speech capture / ASR locale | [[On-Device AI]], [[Architecture]] |
| Screens / theme / navigation / context menus | [[UI and Design]] (+ [[TrailMix Overview]] screens table) |
| Manifest / permissions / posture / data egress | [[Security and Privacy]] |
| Gradle / build / install / release | [[Build and Deployment]] |
| Anything user-facing | Repo `README.md` — and re-verify it's accurate every session, not just when it changes |
| Every session | [[Future Improvements]] To-Do/Complete; version bump (`app/build.gradle.kts` versionName/versionCode) for feature batches; commit (push once a remote exists) |
| This prompt itself | Keep vault [[Master Prompt]] and repo `CLAUDE.md` identical |

## Backlog

Work open rows in the **To-Do table** in [[Future Improvements]] (priority then table order) unless directed otherwise. Check the **Declined list** at the top of that file before proposing anything resembling it. Items whose Details say "user decision" get sign-off **before implementation**.

**Filing new work.** Any new work item — from a user request, a bug report, or an idea — must become a new row in the To-Do table using the exact schema `| ID | Priority | Category | Item | Details | Requested |`. Model the wording on a good existing row: terse but complete: what's broken/missing, what the fix would touch, any constraint that matters. It must get a fresh, permanent ID using one of the existing prefixes (`REL-`, `CAP-`, `AI-`, `UX-`, `SEC-`, `LIN-`, `OBS-`, `CAL-`, `INT-`) or a new prefix if it's genuinely a new category — **never** left as an untabled bullet, **never** reusing or renumbering an ID even after the row ships or is declined (old session notes and code comments reference IDs permanently). A **"Feature Ideas & Innovation Opportunities"** section (bulleted, not tabled) is the only acceptable place for a not-yet-triaged idea to sit outside the To-Do table — and even there, the moment someone decides to act on it, promote it to a proper To-Do row with an ID (mark the bullet `*Promoted:* see **ID** in To-Do` or delete it) rather than building it while still a bullet.

Ship a row → move it to Complete with version/date. If an audit turns up a row that's already implemented but was never formally shipped through this process, move it to Complete too, marked with an honest `found complete during <date> audit` note in place of a version — don't fabricate a version/date it doesn't have.

## Extension Protocol

New capability → new module section + registry row here (and in the repo `CLAUDE.md` mirror); don't stretch an existing module. Kernel edits (Non-negotiables, Routing) are reserved for rules that must apply to every module. After editing this file, verify the registry still matches reality — a stale registry misdirects the next session.
