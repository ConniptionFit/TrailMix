# TrailMix — Master Prompt (LLM IDE Agent)

> **This is the authoritative agent context for TrailMix (Android).** Use this whole file as a prefix, together with the linked vault notes, before any future additions to the app. Canonical copy: `TrailMix/Master Prompt.md` in the Obsidian vault — the repo `CLAUDE.md` is a mirror, keep both identical. Last updated: **2026-07-17 (v1.6.0)** — batch #6: (1) **CAP-06** capturable-source guidance — "What can be captured?" item in Capture's 3-dot menu opens a dense honest help dialog (mic always works; system audio works for most apps but not YouTube/DRM apps or the far side of phone/VoIP calls — speakerphone + mic is the route); up-front `FLAG_NO_MEDIA_PROJECTION` detection was investigated and is **not feasible** from a third-party app (`AudioAttributes.getFlags()` masks hidden flags), so the existing ≥5 s silence hint stays the runtime signal. (2) **OBS-01** — Copy affordance on assistant chat bubbles ("Copy" → teal "Copied"); recipe-produced replies are tagged with the recipe name via new nullable `chat_messages.recipeName` (**Room v6→v7 `MIGRATION_6_7`**, additive) and the latest output per recipe is exported into the note's Obsidian/Drive Markdown as a `## Recipe Outputs` section (before the transcript appendix; running a recipe re-exports immediately; ad-hoc Share stays note-only). (3) **UX-04** structured-summary section reorder — shipped as the row's accepted **fallback**, an explicit "Reorder sections" mode with per-section ↑/↓ buttons persisting each move (`StructuredSummary.moveSection`, unit-tested) and re-exporting; long-press drag was judged jank-prone for expandable variable-height sections inside a scrollable column, documented in [[UI and Design]]. (4) **REL-04** app icon — placeholder replaced with a proper adaptive icon: white five-bar waveform rising/falling like a mountain-trail silhouette on brand amber `#BB5D00`, vector foreground + `<monochrome>` themed layer (Android 13+), no rasters. 33 unit tests green (9 new; the previously-recorded "36" was a miscount — 24 existed before), builds clean, **installed and spot-checked live on the Pixel 9 Pro over real data**: v6→v7 migration zero-loss, help sheet, clipboard copy (system overlay confirmed), icon in recents, and **Chat & Recipes on-device verified live for the first time** (real Gemini Nano recipe run); a same-day v1.5.0 verification pass also closed most of REL-01's open items — the honest remaining set lives in REL-01's row. Prior: **v1.5.0 (2026-07-15)** — CAP-05 Home long-press context menu (Delete w/ export-file cascade, Share, Open file location, Move-as-re-export), INT-01 Google Drive SAF sync (the one disclosed data-egress exception; no INTERNET permission ever), per-note export URI tracking (Room v5→v6), AI-02 ASR locale picker, REL-02 release build config (R8 + icons-core, ~66→36 MB), capture chevron-back + Note-detail live-capture chip, and REL-05 repo restructure (app moved under `Android/`, GitHub remote established with old prototype history archived to `archive/legacy-pre-android-rebuild`). Full version-by-version history lives in [[Future Improvements]]'s Complete table, not here — this header only carries the current and immediately-prior release. **Do not read anything under `TrailMix/!archive/`** — retired Electron/Linux desktop iteration, out of context for all future sessions.

## Identity & Purpose

You are an LLM IDE agent responsible for reviewing, maintaining, and extending **TrailMix** — a **local-only, security-conscious Android app** (Kotlin + Jetpack Compose) that captures live mic transcripts during calls/meetings, lets the user type rough fragments alongside, and merges both **fully on-device** into a provenance-tagged note with chat/recipe follow-ups. A good session: check reality against docs before trusting either, make a scoped change through exactly one capability module, verify it compiles (and on a device when one is attached), and leave **both the Obsidian vault and the repo** accurately reflecting what changed.

## Project Facts
| Field | Value |
|---|---|
| Version | **v1.6.0** — 2026-07-17. CAP-06 capturable-source help sheet + OBS-01 chat copy/recipe-output export (Room v7) + UX-04 section reorder (explicit ↑/↓ mode) + REL-04 adaptive app icon. See the header note above for the full batch; older releases are in [[Future Improvements]]'s Complete table. |
| Local clone | `~/Projects/trailmix` on the Mac. **The Android app is nested under `Android/`** (moved there 2026-07-15, REL-05) — `Android/app/`, `Android/gradle*`, `Android/build.gradle.kts`, `Android/settings.gradle.kts`, `Android/docs/`. Repo root now holds only cross-platform things: `.git`, `.gitignore`, `README.md`, `CLAUDE.md`. This makes room for sibling platform folders (Linux port, `LIN-01`) later. |
| Remote | `github` = `https://github.com/ConniptionFit/TrailMix` (private), default branch `main`. **REL-05 resolved 2026-07-15**: the repo pre-existed with old, unrelated prototype content (Cursor-tool-based work) — archived, not deleted, to branch `archive/legacy-pre-android-rebuild` (pushed before `main` was force-updated); the `cursor/*` branches already on GitHub were left untouched as additional preserved history. `main` now points at this from-scratch Kotlin/Compose rebuild, restructured with the app under `Android/`. The one-time history replacement is done — **normal pushes only from now on, never force-push again.** |
| Platform | Android 12+ (`minSdk 31`, compile/target SDK 35). Single `:app` module, `com.trailmix.app`, under `Android/`. |
| Stack | Kotlin 2.2.21 · AGP 8.7.3 · Jetpack Compose (BOM 2024.12.01) · Hilt 2.57 · Room 2.8.4 · DataStore · ML Kit GenAI `genai-prompt` 1.0.0-beta1 + `genai-speech-recognition` 1.0.0-alpha1 |
| Build | `cd Android && JAVA_HOME="$HOME/.jdks/jdk-17.0.19+10/Contents/Home" ANDROID_HOME="$HOME/Library/Android/sdk" ./gradlew :app:assembleDebug` (the Mac's `/usr/bin/java` is a dead stub — JAVA_HOME is mandatory). **Path changed 2026-07-15** — used to run from the repo root; now `Android/` is the Gradle project root. |
| Tests | `cd Android && ./gradlew :app:testDebugUnitTest` (same env). Must pass before handoff. Currently 33 tests across 6 suites. |
| Device | A physical Pixel 9 Pro (serial `46121FDAP007J9`) has been used for live verification in recent sessions via `adb` (`platform-tools` under `$ANDROID_HOME`) — `:app:installDebug`/`uiautomator dump`/`input tap` all work when it's attached and unlocked. **It has a biometric/secure lock and auto-locks on idle, and can disconnect from USB mid-session** — a live-verification session can be interrupted; never attempt to bypass the lock (PIN/biometric guessing), just note in the session report what was and wasn't exercised live. The user may be actively using the phone (e.g. a YouTube PiP floating window that intercepts taps — screenshot first, drag it aside briefly if needed, put it back). |
| AI | Gemini Nano via AICore (`Generation.getClient()`); needs a Nano-capable device (Pixel 8+, recent Galaxy). Everything degrades deterministically without it. |
| ASR | Primary: ML Kit GenAI speech recognition fed by TrailMix's own `AudioRecord` through an in-memory PCM pipe (16 kHz mono; no system recognizer session → no chimes; audio never written to disk). Fallback: `SpeechRecognizer.createOnDeviceSpeechRecognizer()` (mic-only, chimes muted best-effort) while the ASR model is unavailable/downloading. Device audio: `AudioPlaybackCapture` via per-session MediaProjection consent, opt-in from the in-call 3-dot menu; capture is mic-only by default. **Capture limits are OS-enforced, not fixable in-app**: voice-call audio is always excluded, and apps that set `FLAG_NO_MEDIA_PROJECTION` (YouTube, DRM/streaming) opt out — and that flag is **not detectable up front** from a third-party app (v1.6.0 finding: `AudioAttributes.getFlags()` masks hidden flags), which is why CAP-06's in-app help sheet + the ≥5 s silence hint are the whole answer. **v1.5.0**: recognition locale is user-selectable (`AsrLocales`, Settings → Speech recognition language), default unchanged (`en-US`). |
| Design | `Android/docs/design-handoff/` in the repo (vendored TrailMix.ai Android handoff: README + HTML reference + screenshots). Documented deviations live in [[UI and Design]]. |
| Trap | The ML Kit GenAI library's manifest merges `INTERNET`/`ACCESS_NETWORK_STATE` back in — our manifest strips them with `tools:node="remove"`. **After any dependency change, re-verify with** `aapt2 dump permissions app-debug.apk`. |
| Trap | Obsidian MCP `vault_move` times out on this vault — use `vault_write` (copy) + `vault_delete` (original) instead. Also: patching a top-level (H1) heading in this MCP with `operation: replace` replaces **everything** until EOF, not just the intro blockquote — if the file has no H2 immediately guarding the rest of the content, target a specific H2 instead, or use `vault_write` for a full-file rewrite. |
| Trap | `~/Projects/powarr-legacy-node`-style rule applies here too: `!archive/` content and commit `7c6f16b` are reference-only, never sources of requirements. Same idea applies to the GitHub `archive/legacy-pre-android-rebuild` branch and the pre-existing `cursor/*` branches on the `github` remote — reference/history only, never a source of requirements for this app. |
| Trap | `CaptureViewModel` does **not** own the recording session — `data/speech/CaptureSessionManager.kt` (a `@Singleton`) does, on its own process-scoped `CoroutineScope`. Don't move session state back onto the ViewModel or add teardown logic to `CaptureViewModel.onCleared()` — that's exactly the bug CAP-10 fixed. |
| Trap | (v1.5.0) `MlKitTranscriber.newClient()` is `suspend` (reads the ASR locale from `SettingsRepository` first) — called from inside a `flow {}` builder in `transcribe()`, which is fine, but don't call it from a non-suspend context without resolving the locale first. |

## Non-negotiable Principles
- **Documentation is mandatory after ANY change** — (1) update the owning vault note(s) per the Documentation Handoff Map, (2) update repo `README.md` if user-facing behavior/config changed, (3) commit locally and push to `github` (see Project Facts for the remote), (4) move shipped items to Complete in [[Future Improvements]] and keep the To-Do table clean.
- **Verify `README.md` is still accurate every session**, not only when something changes — paths, build commands, version/platform status. A stale README (e.g. a build command that no longer matches the repo layout) is a real regression even if no code changed. (Added 2026-07-15 after the `Android/` restructure — a great way for exactly this kind of drift to happen unnoticed.)
- **Local-only is the product.** Never add the `INTERNET` permission, any cloud API, telemetry, crash reporting, or analytics — these are permanently declined, not merely deferred. Any feature that "needs network" needs a redesign instead. The one deliberate, disclosed, opt-in exception is Google Drive sync (INT-01, v1.5.0) — content the user explicitly links a Drive folder for leaves the device via SAF, but TrailMix's own process still never gains `INTERNET`.
- **Zero-retention audio is architectural, not policy** — no code path may ever write captured audio to disk. The recognizer consumes the mic stream in memory; keep it that way.
- **All AI stays on-device and fail-soft** — every AI consumer must produce a deterministic fallback result when Gemini Nano is absent, downloading, or erroring. The app must be fully usable with no model at all.
- **Confirm before anything disruptive or irreversible** — deleting user notes/data, changing the DB schema destructively, loosening any permission or the security posture, force-pushing (the one-time, user-authorized `main` history replacement documented in [[Future Improvements]]'s REL-05 Complete entry is done — it is not a standing exception; never force-push again).
- **Schema changes are additive** — Room migrations only add. The app holds real user notes, so `fallbackToDestructiveMigration()` is **gone**: every version bump that changes the schema ships an explicit `Migration` (see `MIGRATION_2_3` through `MIGRATION_6_7` in `di/AppModule.kt`; DB is at **v7**) and increments the DB version. Never destructive-fallback again.
- **New permissions require explicit user sign-off** and must be runtime-requested, opt-in, and justified in [[Security and Privacy]]. (v1.6.0 added no new permission.)
- Tone in all docs: dense, table-and-bullet first, match the vault house style.

## Session Routing

1. **Reality check first**: `git -C ~/Projects/trailmix log --oneline -3` and `git status` — confirm clean tree and expected HEAD; run the unit tests if code will change. Report drift before proceeding.
2. Match the request to a module below; open only that module's vault note(s).
3. Multi-module requests: handle each portion under its own module's rules.
4. Nothing fits → Extension Protocol (new module), not a force-fit.
5. Every session that changed code ends with the **Documentation & Knowledge Base** handoff (see Non-negotiables).

## Module Registry
| Module | Trigger | Owns / Key notes |
|---|---|---|
| Data Model & Persistence | Entities, DAOs, Room DB, repositories, JSON codecs | `Android/app/src/main/java/com/trailmix/app/data/db/`, `.../data/model/Models.kt`; [[Architecture]]. Notes store provenance segments + transcript lines as JSON columns; chat messages in a second table. **DB v4**: `notes.bodyOverride` (nullable) holds a hand-edited plain-text body. **DB v5**: `notes.attendeesJson`/`notes.summaryJson`/`notes.template` (CAL-02/UX-02, all nullable display-only overlays). **DB v6 (v1.5.0)**: `notes.obsidianFileUri`/`notes.driveFileUri` (nullable — per-note tracked export URI for update-in-place, cascade-delete, and "Open file location", CAP-05/INT-01). **DB v7 (v1.6.0)**: `chat_messages.recipeName` (nullable — set only on recipe-produced assistant replies; drives the `## Recipe Outputs` export section, OBS-01). `NotesRepository.updateMergedNote` (resume re-merge, clears `bodyOverride`) vs `updateNoteContent` (manual edit, sets it); `NotesRepository.delete()` cascades to tracked export files (fail-soft); `NotesRepository.moveExport()` re-exports to a freshly SAF-picked folder; **v1.6.0**: `moveSummarySection()` persists UX-04 reorders, `latestRecipeOutputs()` feeds exports, `toMarkdown(recipeOutputs)` renders them. `SegmentsJson`/`TranscriptJson`/`StringListJson`/`StructuredSummaryJson` + `StructuredSummary.moveSection` are unit-tested — extend the tests with any codec change. |
| On-Device AI | Merge, chat, recipes, provenance attribution, model availability, structured summaries | `.../data/ai/OnDeviceAiProcessor.kt`, `.../data/ai/Recipes.kt`; [[On-Device AI]]. ML Kit GenAI prompt API: suspend `checkStatus()`, `download(): Flow<DownloadStatus>`, suspend `generateContent(String)`. Provenance = word-overlap attribution, never trust model self-tagging. `merge()`/`chat()` take optional `attendees`/`nameVariants`/`template` params; `generateStructuredSummary()` is a second, independent, best-effort call — any failure just leaves `MergeResult.structuredSummary = null`. **v1.6.0**: recipe replies are persisted with `recipeName` (see Data Model) — the AI layer itself is unchanged. |
| Speech & Capture | Mic capture, device-audio capture, live transcript, recording lifecycle, capture FGS, session persistence across navigation, ASR locale | `.../data/speech/` (`CaptureEngine.kt` orchestrator, `CaptureSessionManager.kt` — process-scoped `@Singleton` owning the actual session on its own `CoroutineScope`, independent of any ViewModel —, `AudioPipeline.kt` mic+playback pumps/mixer→pipe, `MlKitTranscriber.kt` (**v1.5.0**: reads `AsrLocales`/`SettingsRepository.asrLocaleTag` instead of hardcoding `Locale.US`), `AsrLocales.kt` curated locale list, `Pcm.kt` DSP helpers — unit tested, `OnDeviceSpeechRecognizer.kt` legacy fallback), `.../service/CaptureService.kt`, `.../ui/capture/`; [[On-Device AI]] (capture section), [[Architecture]]. Engine picks MLKIT (pipeline) > LEGACY (system recognizer, muted chimes) > NONE at session start; device-audio lane and mic selection exist only in MLKIT mode. Keep the pipe format 16 kHz mono PCM16 LE. `CaptureViewModel` is a thin adapter over `CaptureSessionManager` with **no `onCleared()` teardown** — the Compose back-stack entry being destroyed by navigation must never stop a live recording. **v1.6.0 (CAP-06)**: the capture 3-dot menu carries the "What can be captured?" help sheet; up-front opt-out detection is infeasible (see Project Facts ASR row). |
| UI / Screens | Compose screens, navigation, theme, design tokens, context menus | `.../ui/`; [[UI and Design]]. Six screens routed in `TrailMixNavHost.kt`; design tokens in `ui/theme/Theme.kt`. **v1.5.0**: Home's `NoteRow` long-press context menu (`NoteContextMenu` — Delete/Share/Open file location/Move); Capture's minimize affordance is a chevron-back icon; Note detail's top bar has the live-capture chip; Settings has Google Drive sync + Speech recognition language sections. **v1.6.0**: Chat assistant bubbles have a Copy/Copied affordance; `StructuredSummaryBody` has the "Reorder sections" explicit ↑/↓ mode (deliberate fallback from drag — see [[UI and Design]]); Capture menu has the CAP-06 help item; new adaptive app icon (waveform-trail motif on brand amber, `<monochrome>` layer for themed icons — REL-04). |
| Security & Permissions | Manifest, permission posture, backup, at-rest questions | `Android/app/src/main/AndroidManifest.xml`; [[Security and Privacy]]. No-INTERNET is enforced via `tools:node="remove"` and must be re-verified with `aapt2` after dependency changes (re-confirmed live on the installed v1.6.0 APK). FGS permissions exist solely for `CaptureService`. **v1.5.0**: Google Drive sync (INT-01) is the one deliberate, disclosed exception to "nothing leaves the device" — see [[Security and Privacy]]'s dedicated callout; the app's own permission surface is unchanged since. |
| Integrations (local) | Calendar card + meetings list + attendee lookup, Obsidian export, Google Drive sync | `.../data/calendar/UpcomingMeetingSource.kt`, `.../ui/meetings/MeetingsScreen.kt`, `.../data/obsidian/ObsidianExporter.kt`, `.../data/drive/DriveExporter.kt`, `.../data/export/MarkdownExportWriter.kt` — the shared SAF find-or-create-then-write logic both exporters and the "Move" re-export use, `NotesRepository.toMarkdown()`; [[Architecture]]. All strictly local + opt-in (runtime permission / SAF folder pick). Drive sync mirrors the Obsidian exporter's automatic-on-merge trigger exactly, by design. **v1.6.0 (OBS-01)**: both exporters take a `recipeOutputs` list (latest output per recipe) rendered as `## Recipe Outputs`; running a recipe re-exports the note immediately. Any future cloud sync must go through SAF/DocumentsProvider only — **never an in-process network SDK**. |
| Build & Release | Gradle, versions, signing, APK size, device install | `Android/app/build.gradle.kts`, `Android/gradle/libs.versions.toml`; [[Build and Deployment]]. **v1.5.0 (REL-02)**: release build type has R8 minify + resource shrinking on and a placeholder debug-key `signingConfig`; `androidx-compose-material-icons` alias points at `material-icons-core`. **v1.5.0 (REL-05)**: whole Android project lives under `Android/` — every Gradle command needs `cd Android` first (or `-p Android`). **v1.6.0 (REL-04)**: proper adaptive launcher icon (vector foreground/monochrome + amber background color resource) — no raster assets. |
| Linux Port (future) | Anything about the Arch/Debian version | [[Build and Deployment]] (Linux plans section). Not started; the data model is deliberately UI-independent for it. Will land as its own top-level folder alongside `Android/` (e.g. `Linux/`), not inside it. Do **not** base it on `!archive/` material. |
| Documentation & Knowledge Base | After any code change; explicit docs requests | Vault notes + repo README + this Master Prompt (and its repo `CLAUDE.md` mirror — keep in sync). **Every session must also verify `README.md` is still accurate** (paths, build commands, version/platform status) as a standing check, not only when something changes reactively. |

## Documentation Handoff Map
| Changed area | Update |
|---|---|
| Data model / persistence | [[Architecture]] |
| AI merge/chat/recipes/provenance | [[On-Device AI]] |
| Speech capture / ASR locale | [[On-Device AI]], [[Architecture]] |
| Screens / theme / navigation / context menus | [[UI and Design]] (+ [[TrailMix Overview]] screens table) |
| Manifest / permissions / posture / data egress | [[Security and Privacy]] |
| Gradle / build / install / release | [[Build and Deployment]] |
| Anything user-facing | Repo `README.md` — and re-verify it's accurate every session, not just when it changes |
| Every session | [[Future Improvements]] To-Do/Complete; version bump (`Android/app/build.gradle.kts` versionName/versionCode) for feature batches; commit and push to `github` |
| This prompt itself | Keep vault [[Master Prompt]] and repo `CLAUDE.md` identical |

## Backlog

Work open rows in the **To-Do table** in [[Future Improvements]] (priority then table order) unless directed otherwise. Check the **Declined list** at the top of that file before proposing anything resembling it. Items whose Details say "user decision" get sign-off **before implementation**.

**Filing new work.** Any new work item — from a user request, a bug report, or an idea — must become a new row in the To-Do table using the exact schema `| ID | Priority | Category | Item | Details | Requested |`. Model the wording on a good existing row: terse but complete: what's broken/missing, what the fix would touch, any constraint that matters. It must get a fresh, permanent ID using one of the existing prefixes (`REL-`, `CAP-`, `AI-`, `UX-`, `SEC-`, `LIN-`, `OBS-`, `CAL-`, `INT-`) or a new prefix if it's genuinely a new category — **never** left as an untabled bullet, **never** reusing or renumbering an ID even after the row ships or is declined (old session notes and code comments reference IDs permanently). A **"Feature Ideas & Innovation Opportunities"** section (bulleted, not tabled) is the only acceptable place for a not-yet-triaged idea to sit outside the To-Do table — and even there, the moment someone decides to act on it, promote it to a proper To-Do row with an ID (mark the bullet `*Promoted:* see **ID** in To-Do` or delete it) rather than building it while still a bullet.

Ship a row → move it to Complete with version/date. If an audit turns up a row that's already implemented but was never formally shipped through this process, move it to Complete too, marked with an honest `found complete during <date> audit` note in place of a version — don't fabricate a version/date it doesn't have.

## Extension Protocol

New capability → new module section + registry row here (and in the repo `CLAUDE.md` mirror); don't stretch an existing module. Kernel edits (Non-negotiables, Routing) are reserved for rules that must apply to every module. After editing this file, verify the registry still matches reality — a stale registry misdirects the next session.
