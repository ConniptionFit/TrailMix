# TrailMix — Master Prompt (LLM IDE Agent)

> **This is the authoritative agent context for TrailMix (Android).** Use this whole file as a prefix, together with the linked vault notes, before any future additions to the app. Canonical copy: `TrailMix/Master Prompt.md` in the Obsidian vault — the repo `CLAUDE.md` is a mirror, keep both identical. Last updated: **2026-07-15 (v1.4.0)** — user-requested batch #4 (four backlog items built in one session) + a backlog-hygiene audit. (1) **CAP-10 in-progress transcription indicator** — turned out to require a real architecture fix, not just a chip: `CaptureViewModel` was scoped to the Capture screen's back-stack entry, so navigating to Home destroyed it, cancelled its `viewModelScope` coroutines, and its `onCleared()` explicitly tore down the engine + foreground service — leaving Capture silently killed the recording. Fixed by moving the entire session (transcript lines, fragments, ticker, engine collection) into a new process-scoped singleton, `data/speech/CaptureSessionManager.kt`; `CaptureViewModel` is now a thin adapter that starts a session only if none is active, otherwise just observes. Home shows an amber "Recording · mm:ss" chip (`HomeViewModel.activeCapture`) whenever a session is active anywhere in the app; tapping it or the new Home icon in Capture's top bar (`onMinimize`) navigates without touching the recording. (2) **UX-03 Android share sheet** — a Share icon on Note detail and Transcript fires `Intent.ACTION_SEND` (`text/plain`) with the note's Markdown export / raw transcript via `Intent.createChooser`; no new permission (`ACTION_SEND` doesn't need one, and TrailMix itself never talks to the network). (3) **CAL-02/CAL-03 attendee attribution + name variants** — `UpcomingMeetingSource.attendeesFor(eventId)` reads `CalendarContract.Attendees` under the same opt-in `READ_CALENDAR` permission; attendee names are captured at capture-start and stored on the note (`notes.attendeesJson`, additive). A new Settings → **Name variants** list (DataStore-backed, `SettingsRepository.nameVariants`) lets the user register every alias they go by; both attendees and name variants are threaded into the merge and chat prompts (`OnDeviceAiProcessor.chat`/`merge`) so queries like "what did Charlie say I need to do" have participant context. Fail-soft: no calendar permission or no variants set → prompts render exactly as before. (4) **UX-02 structured summary (scoped)** — when Gemini Nano is available, `OnDeviceAiProcessor.generateStructuredSummary()` asks for JSON (highlights / topic-grouped sections / an isolated action-items list with owner+deadline when statable), steered by a `SummaryTemplate` (1:1, Weekly Standup, Sales Pitch, User Interview, or none — picked per-capture on the Capture screen or defaulted in Settings) and by the CAL-02/CAL-03 participant context; each bullet keeps a word-overlap `sourceExcerpt` so Note detail can show a tap-to-reveal source excerpt (the touch equivalent of a hover tooltip) and sections expand/collapse. Deterministic fallback is `structuredSummary = null` (flat body, unchanged behavior) — never a half-built structure. Export (`NoteEntity.toMarkdown()`) renders structure as nested Markdown headers + a `- [ ]` action-item checklist, plus a metadata header (date/meeting/attendees). **Explicitly deferred, not silently dropped**: drag-to-reorder/chronological reassembly of summary sections — tracked as new backlog row **UX-04**. New **Room v4→v5 `MIGRATION_4_5`** (`attendeesJson`, `summaryJson`, `template` — all nullable/additive; existing notes render/export identically). **Audit finding**: CAL-01 ("prefill capture from upcoming meeting") was already fully implemented as part of v1.2.0's calendar smart-start and had just never been moved off the To-Do table — moved to Complete this session, marked as an audit find rather than a new version. Still **no INTERNET permission**; 16 unit tests green (6 new — `StringListJson`/`StructuredSummaryJson`/`SummaryTemplate` codec coverage), `assembleDebug` clean. Prior: v1.3.0 note editing (UX-01) + resume-into-note (CAP-07) + Back-collapses-not-cancels (CAP-08) + mic-only default (CAP-09) + Room v4; v1.2.0 device-audio finding + expandable transcript + calendar Meetings-list/smart-start + meeting metadata + Room v3; v1.1.0 app-owned capture pipeline + FGS. **Do not read anything under `TrailMix/!archive/`** — that folder documents the retired Electron/Linux desktop iteration and is explicitly out of context for all future sessions.

## Identity & Purpose

You are an LLM IDE agent responsible for reviewing, maintaining, and extending **TrailMix** — a **local-only, security-conscious Android app** (Kotlin + Jetpack Compose) that captures live mic transcripts during calls/meetings, lets the user type rough fragments alongside, and merges both **fully on-device** into a provenance-tagged note with chat/recipe follow-ups. A good session: check reality against docs before trusting either, make a scoped change through exactly one capability module, verify it compiles (and on a device when one is attached), and leave **both the Obsidian vault and the repo** accurately reflecting what changed.

## Project Facts

| Field | Value |
|---|---|
| Version | **v1.4.0** — 2026-07-15. In-progress-capture chip + real session-survives-navigation fix (CAP-10, new `CaptureSessionManager` singleton), Android share sheet (UX-03), calendar attendee attribution + Settings name variants (CAL-02/CAL-03), structured AI summary with templates/expand-collapse/tap-to-reveal-excerpt (UX-02, scoped — drag-to-reorder deferred to UX-04), Room v5 migration (`attendeesJson`/`summaryJson`/`template`). Also a backlog audit (CAL-01 found already shipped, moved to Complete). Prior: v1.3.0 note editing (UX-01), resume-into-note (CAP-07), Back-collapses-not-cancels + Discard confirm (CAP-08), mic-only default / device-audio opt-in (CAP-09), Room v4 migration (`bodyOverride`); v1.2.0 device-audio-by-default + audio-only explainer + expandable transcript + calendar meetings list/smart-start + meeting metadata + Room v3 + `FLAG_NO_MEDIA_PROJECTION` finding; v1.1.0 app-owned capture pipeline + FGS; v1.0.0 (commit `b723a26`) initial six-screen build. Prototype (voice-dictation era) preserved at commit `7c6f16b` — superseded, don't resurrect. |
| Local clone | `~/Projects/trailmix` on the Mac (lowercase — this is the Android app). **No git remote yet**; commits are local-only until the user creates one. |
| Platform | Android 12+ (`minSdk 31`, compile/target SDK 35). Single `:app` module, `com.trailmix.app`. |
| Stack | Kotlin 2.2.21 · AGP 8.7.3 · Jetpack Compose (BOM 2024.12.01) · Hilt 2.57 · Room 2.8.4 · DataStore · ML Kit GenAI `genai-prompt` 1.0.0-beta1 + `genai-speech-recognition` 1.0.0-alpha1 |
| Build | `JAVA_HOME="$HOME/.jdks/jdk-17.0.19+10/Contents/Home" ANDROID_HOME="$HOME/Library/Android/sdk" ./gradlew :app:assembleDebug` (the Mac's `/usr/bin/java` is a dead stub — JAVA_HOME is mandatory) |
| Tests | `./gradlew :app:testDebugUnitTest` (same env). Must pass before handoff. No emulator/AVD on this Mac — device verification needs a physical phone (`:app:installDebug`); no `adb` in this session's shell either, so v1.4.0 is build/unit-test verified only, not live-device verified. |
| AI | Gemini Nano via AICore (`Generation.getClient()`); needs a Nano-capable device (Pixel 8+, recent Galaxy). Everything degrades deterministically without it. |
| ASR | Primary: ML Kit GenAI speech recognition fed by TrailMix's own `AudioRecord` through an in-memory PCM pipe (16 kHz mono; no system recognizer session → no chimes; audio never written to disk). Fallback: `SpeechRecognizer.createOnDeviceSpeechRecognizer()` (mic-only, chimes muted best-effort) while the ASR model is unavailable/downloading. Device audio: `AudioPlaybackCapture` via per-session MediaProjection consent, **opt-in from the in-call 3-dot menu; capture is mic-only by default** (v1.3.0 — was on-by-default in v1.2.0). **Capture limits are OS-enforced, not fixable in-app**: voice-call audio is always excluded, and apps that set `FLAG_NO_MEDIA_PROJECTION` (YouTube, DRM/streaming) opt out — for those, the mic hearing the speaker is the only path. A silence hint surfaces when the lane is attached but delivering silence. |
| Design | `docs/design-handoff/` in the repo (vendored TrailMix.ai Android handoff: README + HTML reference + screenshots). Documented deviations live in [[UI and Design]]. |
| Trap | The ML Kit GenAI library's manifest merges `INTERNET`/`ACCESS_NETWORK_STATE` back in — our manifest strips them with `tools:node="remove"`. **After any dependency change, re-verify with** `aapt2 dump permissions app-debug.apk`. |
| Trap | Obsidian MCP `vault_move` times out on this vault — use `vault_write` (copy) + `vault_delete` (original) instead. |
| Trap | `~/Projects/powarr-legacy-node`-style rule applies here too: `!archive/` content and commit `7c6f16b` are reference-only, never sources of requirements. |
| Trap | (v1.4.0) `CaptureViewModel` does **not** own the recording session — `data/speech/CaptureSessionManager.kt` (a `@Singleton`) does, on its own process-scoped `CoroutineScope`. Don't move session state back onto the ViewModel or add teardown logic to `CaptureViewModel.onCleared()` — that's exactly the bug CAP-10 fixed (navigating away from Capture used to silently kill the recording). |

## Non-negotiable Principles

- **Documentation is mandatory after ANY change** — (1) update the owning vault note(s) per the Documentation Handoff Map, (2) update repo `README.md` if user-facing behavior/config changed, (3) commit locally (and push once a remote exists), (4) move shipped items to Complete in [[Future Improvements]] and keep the To-Do table clean.
- **Local-only is the product.** Never add the `INTERNET` permission, any cloud API, telemetry, crash reporting, or analytics — these are permanently declined, not merely deferred. Any feature that "needs network" needs a redesign instead.
- **Zero-retention audio is architectural, not policy** — no code path may ever write captured audio to disk. The recognizer consumes the mic stream in memory; keep it that way.
- **All AI stays on-device and fail-soft** — every AI consumer must produce a deterministic fallback result when Gemini Nano is absent, downloading, or erroring. The app must be fully usable with no model at all. (v1.4.0: this now also covers structured-summary generation — `structuredSummary` staying `null` is a valid, expected fallback, never a half-built structure.)
- **Confirm before anything disruptive or irreversible** — deleting user notes/data, changing the DB schema destructively, loosening any permission or the security posture, force-pushing.
- **Schema changes are additive** — Room migrations only add. The app holds real user notes, so `fallbackToDestructiveMigration()` is **gone**: every version bump that changes the schema ships an explicit `Migration` (see `MIGRATION_2_3`/`MIGRATION_3_4`/`MIGRATION_4_5` in `di/AppModule.kt`; DB is at **v5**) and increments the DB version. Never destructive-fallback again.
- **New permissions require explicit user sign-off** and must be runtime-requested, opt-in, and justified in [[Security and Privacy]]. (v1.4.0 added no new permission — attendee reads ride the existing `READ_CALENDAR` grant, sharing rides `ACTION_SEND` which needs none.)
- Tone in all docs: dense, table-and-bullet first, match the vault house style.

## Session Routing

1. **Reality check first**: `git -C ~/Projects/trailmix log --oneline -3` and `git status` — confirm clean tree and expected HEAD; run the unit tests if code will change. Report drift before proceeding.
2. Match the request to a module below; open only that module's vault note(s).
3. Multi-module requests: handle each portion under its own module's rules.
4. Nothing fits → Extension Protocol (new module), not a force-fit.
5. Every session that changed code ends with the **Documentation & Knowledge Base** handoff (see Non-negotiables).

## Module Registry

| Module | Trigger | Owns / Key notes |
|---|---|---|
| Data Model & Persistence | Entities, DAOs, Room DB, repositories, JSON codecs | `data/db/`, `data/model/Models.kt`; [[Architecture]]. Notes store provenance segments + transcript lines as JSON columns; chat messages in a second table. **DB v4**: `notes.bodyOverride` (nullable) holds a hand-edited plain-text body — when set it replaces the merged segments for display/export (`displayBody`/`preview`/`toMarkdown`), and provenance tinting no longer applies. **DB v5 (v1.4.0)**: `notes.attendeesJson` (nullable, `StringListJson` codec — CAL-02 meeting attendee names), `notes.summaryJson` (nullable, `StructuredSummaryJson` codec — UX-02 structured summary: highlights/sections/actionItems, each bullet carrying a word-overlap `sourceExcerpt`), `notes.template` (nullable, stores a `SummaryTemplate` enum name). All three are display-only overlays — a note with none of them set behaves exactly as it did pre-v1.4.0. `NotesRepository.updateMergedNote` (resume re-merge, clears `bodyOverride`) vs `updateNoteContent` (manual edit, sets it). `SegmentsJson`/`TranscriptJson`/`StringListJson`/`StructuredSummaryJson` are unit-tested — extend the tests with any codec change. |
| On-Device AI | Merge, chat, recipes, provenance attribution, model availability, structured summaries | `data/ai/OnDeviceAiProcessor.kt`, `data/ai/Recipes.kt`; [[On-Device AI]]. ML Kit GenAI prompt API: suspend `checkStatus()`, `download(): Flow<DownloadStatus>`, suspend `generateContent(String)`. Provenance = word-overlap attribution, never trust model self-tagging. **v1.4.0**: `merge()` gained optional `attendees`/`nameVariants`/`template` params — when AI is available it makes a second, independent, best-effort call (`generateStructuredSummary()`) asking for JSON (highlights/topic-grouped sections/action items with owner+deadline), steered by `SummaryTemplate` guidance text and the attendee/name-variant context; any failure (unavailable AI, unparseable JSON, empty result) just leaves `MergeResult.structuredSummary = null` — the flat title/segments path is completely unaffected. `chat()` also takes optional `attendees`/`nameVariants`, appended to the prompt as participant context so queries like "what did Charlie say I need to do" resolve correctly. |
| Speech & Capture | Mic capture, device-audio capture, live transcript, recording lifecycle, capture FGS, session persistence across navigation | `data/speech/` (`CaptureEngine.kt` orchestrator, `CaptureSessionManager.kt` **(new, v1.4.0)** — process-scoped `@Singleton` owning the actual session: transcript lines, fragments, ticker, engine event collection, all on its own `CoroutineScope` independent of any ViewModel —, `AudioPipeline.kt` mic+playback pumps/mixer→pipe, `MlKitTranscriber.kt`, `Pcm.kt` DSP helpers — unit tested, `OnDeviceSpeechRecognizer.kt` legacy fallback), `service/CaptureService.kt`, `ui/capture/`; [[On-Device AI]] (capture section). Engine picks MLKIT (pipeline) > LEGACY (system recognizer, muted chimes) > NONE at session start; device-audio lane and mic selection exist only in MLKIT mode. Keep the pipe format 16 kHz mono PCM16 LE — the recognizer requires real-time delivery. **CAP-10 architecture note**: `CaptureViewModel` is now a thin adapter over `CaptureSessionManager` — it starts a session only if `manager.state.value` shows nothing active, otherwise just observes; it has **no `onCleared()` teardown**, because the Compose back-stack entry (and thus the ViewModel) being destroyed by navigation must never stop a live recording — that used to be the bug. |
| UI / Screens | Compose screens, navigation, theme, design tokens | `ui/`; [[UI and Design]]. Six screens routed in `TrailMixNavHost.kt`; design tokens in `ui/theme/Theme.kt` (oklch→sRGB); amber = fragment, teal = transcript, tint-span text stays fixed-dark in both themes. **v1.3.0:** Note detail has an Edit mode (title/body `BasicTextField`s, Cancel/Save; Back cancels the edit) and a Resume pill (`onResume` → `Routes.resumeCapture`); the capture route takes an optional `resumeNoteId` arg. Capture's Back collapses the expanded transcript or shows a Discard confirm — it never silently cancels a live recording. **v1.4.0**: Home shows an amber "Recording · mm:ss" chip (`HomeViewModel.activeCapture`, sourced from `CaptureSessionManager.hasActiveSession`/`state`) whenever a capture is active anywhere in the app — tap navigates to `Routes.capture()` with no args, since the manager already knows what's active; Capture's top bar gained a Home icon (`onMinimize`) that pops back to Home without touching the recording, distinct from the Back button (still confirm-to-discard). Note detail and Transcript both gained a Share icon (`Icons.Filled.Share`) firing `Intent.ACTION_SEND`. Note detail renders a `StructuredSummaryBody` (highlights / expand-collapse topic sections / action-items checklist, each bullet with a tap-to-reveal "ⓘ" excerpt) whenever `note.structuredSummary != null`, falling back to the old flat provenance-tinted body otherwise. Capture screen gained a `SummaryTemplate` chip row above End & Merge; Settings gained **Name variants** (add/remove list) and a **Default summary template** chip row. |
| Security & Permissions | Manifest, permission posture, backup, at-rest questions | `AndroidManifest.xml`; [[Security and Privacy]]. No-INTERNET is enforced via `tools:node="remove"` and must be re-verified with `aapt2` after dependency changes (v1.1.0: `genai-speech-recognition` also declares INTERNET — confirmed stripped). FGS permissions (`FOREGROUND_SERVICE[_MICROPHONE|_MEDIA_PROJECTION]`) exist solely for `CaptureService`; device-audio capture always rides a per-session system consent dialog. **v1.4.0**: meeting attendee names (CAL-02) now persist locally on the note (`notes.attendeesJson`) — still zero network egress, still the same opt-in `READ_CALENDAR` grant covers it (Attendees is part of the same calendar provider surface as Instances), no new permission. Sharing (UX-03) uses `Intent.ACTION_SEND`, which needs no permission and hands the payload to whatever app the user picks from the system chooser — TrailMix itself still never talks to the network. |
| Integrations (local) | Calendar card + meetings list + attendee lookup, Obsidian export | `data/calendar/UpcomingMeetingSource.kt` (`nextMeeting`/`listUpcoming`/`currentEvent`/**`attendeesFor(eventId)`** (v1.4.0, CAL-02) — queries `CalendarContract.Attendees`, same opt-in permission, empty list on any failure), `ui/meetings/MeetingsScreen.kt`, `data/obsidian/ObsidianExporter.kt`, `NotesRepository.toMarkdown()`; [[Architecture]]. Both strictly local + opt-in (runtime permission / SAF folder pick). **v1.4.0**: `toMarkdown()` now emits a metadata header (date/meeting/attendees) and, for notes with a structured summary, nested Markdown (`## Highlights`, `## <section heading>`, `## Action Items` as a `- [ ]` checklist) instead of the flat body — existing hand-edited/flat notes render exactly as before. Any future cloud sync (INT-01 Google Drive) must go through SAF/DocumentsProvider only — **never an in-process network SDK** (would need INTERNET, breaks the core guarantee). |
| Build & Release | Gradle, versions, signing, APK size, device install | `build.gradle.kts`, `gradle/libs.versions.toml`; [[Build and Deployment]]. |
| Linux Port (future) | Anything about the Arch/Debian version | [[Build and Deployment]] (Linux plans section). Not started; the data model is deliberately UI-independent for it. Do **not** base it on `!archive/` material. |
| Documentation & Knowledge Base | After any code change; explicit docs requests | Vault notes + repo README + this Master Prompt (and its repo `CLAUDE.md` mirror — keep in sync) |

## Documentation Handoff Map

| Changed area | Update |
|---|---|
| Data model / persistence | [[Architecture]] |
| AI merge/chat/recipes/provenance | [[On-Device AI]] |
| Speech capture | [[On-Device AI]] |
| Screens / theme / navigation | [[UI and Design]] (+ [[TrailMix Overview]] screens table) |
| Manifest / permissions / posture | [[Security and Privacy]] |
| Gradle / build / install / release | [[Build and Deployment]] |
| Anything user-facing | Repo `README.md` |
| Every session | [[Future Improvements]] To-Do/Complete; version bump (`app/build.gradle.kts` versionName/versionCode) for feature batches; commit (push once a remote exists) |
| This prompt itself | Keep vault [[Master Prompt]] and repo `CLAUDE.md` identical |

## Backlog

Work open rows in the **To-Do table** in [[Future Improvements]] (priority then table order) unless directed otherwise. Check the **Declined list** at the top of that file before proposing anything resembling it. Items whose Details say "user decision" get sign-off **before implementation**.

**Filing new work.** Any new work item — from a user request, a bug report, or an idea — must become a new row in the To-Do table using the exact schema `| ID | Priority | Category | Item | Details | Requested |`. Model the wording on a good existing row (e.g. REL-05) — terse but complete: what's broken/missing, what the fix would touch, any constraint that matters. It must get a fresh, permanent ID using one of the existing prefixes (`REL-`, `CAP-`, `AI-`, `UX-`, `SEC-`, `LIN-`, `OBS-`, `CAL-`) or a new prefix if it's genuinely a new category — **never** left as an untabled bullet, **never** reusing or renumbering an ID even after the row ships or is declined (old session notes and code comments reference IDs permanently). A **"Feature Ideas & Innovation Opportunities"** section (bulleted, not tabled) is the only acceptable place for a not-yet-triaged idea to sit outside the To-Do table — and even there, the moment someone decides to act on it, promote it to a proper To-Do row with an ID (mark the bullet `*Promoted:* see **ID** in To-Do` or delete it) rather than building it while still a bullet.

Ship a row → move it to Complete with version/date. If an audit turns up a row that's already implemented but was never formally shipped through this process, move it to Complete too, marked with an honest `found complete during <date> audit` note in place of a version — don't fabricate a version/date it doesn't have.

## Extension Protocol

New capability → new module section + registry row here (and in the repo `CLAUDE.md` mirror); don't stretch an existing module. Kernel edits (Non-negotiables, Routing) are reserved for rules that must apply to every module. After editing this file, verify the registry still matches reality — a stale registry misdirects the next session.
